apiVersion: v1
kind: ServiceAccount
metadata:
  name: workaholic
  namespace: workaholic
  annotations:
    # Bind an IAM role that can describe EKS/CloudWatch/ASG. Replace the ARN.
    eks.amazonaws.com/role-arn: arn:aws:iam::123456789012:role/workaholic
